import pgzrun #python game zero run
from random import randint

WIDTH=400
HEIGHT=400
TITLE="Shapes"

x=20
y=20
w=360
h=360

r=randint(0,255)
g=randint(0,255)
b=randint(0,255)

def draw():

    x=200
    y=200
    w=360
    h=360

    radius=180

    r=randint(0,255)
    g=randint(0,255)
    b=randint(0,255)
    c=(r,g,b)
    screen.fill("skyblue")
    for i in range(13):
        #(x,y),(w,h),color
        
        screen.draw.filled_circle((x,y),radius,c)        
        r=randint(0,255)
        g=randint(0,255)
        b=randint(0,255) 
        c=(r,g,b)
        radius-=20


pgzrun.go()
